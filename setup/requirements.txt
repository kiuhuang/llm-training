# Core LLM training stack (install torch separately first — see setup_env.sh)
# Versions chosen for: Qwen3.5-4B (model_type qwen3_5), H800 (SM90) GPUs,
# CUDA 12.4 wheels. NOTE: the qwen3_5 architecture ships in transformers 5.x
# ONLY (>=5.16.1) — 4.57.x does not recognize it. That also means the host
# venv needs python >= 3.10; on old hosts use the container (docs/08).

# --- Core ---
transformers>=5.16.1,<6     # qwen3_5 architecture support + chat templates
datasets>=2.20.0            # streaming + map/filter for Finance-Instruct-500k
accelerate>=0.33.0          # multi-GPU DDP launch, mixed precision
peft>=0.12.0                # LoRA / QLoRA adapters
trl>=0.9.6                  # SFTTrainer (prompt/completion masking, packing)
sentencepiece>=0.1.99       # tokenizer deps for some models
tokenizers>=0.19.0
safetensors>=0.4.3
pyyaml>=6.0
numpy<2.1                   # avoid numpy 2.x incompat with some libs
tqdm>=4.66

# --- Distributed / optimization ---
deepspeed>=0.14.4           # ZeRO-2/3 for full-parameter SFT (optional but recommended)
bitsandbytes>=0.43.1        # 4-bit QLoRA option

# --- Logging & metrics ---
tensorboard>=2.16           # NGC 25.03 base ships 2.16.2 — don't force newer
rouge-score>=0.1.2          # ROUGE-L for eval comparison
absl-py                     # rouge-score dependency
scikit-learn>=1.4.0         # stratified split / metrics helpers
matplotlib>=3.8.0           # eval charts (optional)

# --- HF hub performance (optional but recommended) ---
huggingface_hub>=0.24.0     # >=1.x: fast Xet transfer is built in (HF_XET_HIGH_PERFORMANCE=1)

# flash-attn is installed separately by setup_env.sh (compiles for ~30-60 min,
# or use a prebuilt wheel). Not listed here on purpose.
